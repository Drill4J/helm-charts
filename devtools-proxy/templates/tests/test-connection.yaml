apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devtools-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "devtools-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devtools-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
